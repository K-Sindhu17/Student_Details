# HTTP only — used locally and for initial Certbot certificate request
server {
    listen 80;
    server_name _;

    # Use Docker's embedded DNS so the backend hostname is re-resolved at request time.
    # Avoids stale upstream IPs when the backend container restarts.
    resolver 127.0.0.11 valid=10s ipv6=off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        set $backend_upstream "backend:5000";
        proxy_pass http://$backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

